# ghostly_apparition
Renders a single-cell time-projection animation using napari. Each timepoint is stacked along the Z axis to build a 3D temporal tower, then screenshotted frame-by-frame and compiled into an MP4. Spooky.
